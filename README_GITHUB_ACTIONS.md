# 🤖 إعداد التحديث التلقائي كل 15 دقيقة (مثل المشروع المرجعي)

## 🎯 الهدف
تحديث ملفات JSON تلقائياً كل 15 دقيقة على GitHub، تماماً مثل المشروع المرجعي `ALAAWF2/orange-dashboard`.

---

## 📋 الخيارات المتاحة

### الخيار 1: GitHub Actions مع قاعدة بيانات بعيدة (موصى به)

**المتطلبات:**
- قاعدة بيانات PostgreSQL بعيدة (مثل Supabase، Railway، AWS RDS)
- إضافة Secrets في GitHub

**الخطوات:**

1. **إضافة Secrets في GitHub:**
   - اذهب إلى: `Settings` → `Secrets and variables` → `Actions`
   - أضف:
     - `PG_HOST` - عنوان قاعدة البيانات البعيدة
     - `PG_DATABASE` - اسم قاعدة البيانات
     - `PG_USER` - اسم المستخدم
     - `PG_PASSWORD` - كلمة المرور
     - `PG_PORT` - المنفذ (افتراضي: 5432)
     - `PG_SSL` - `true` للإنتاج

2. **تفعيل Workflow:**
   - الملف: `.github/workflows/update-json-15m.yml`
   - سيعمل تلقائياً كل 15 دقيقة

---

### الخيار 2: Self-Hosted Runner (للقاعدة المحلية)

**المتطلبات:**
- جهاز محلي يعمل 24/7
- قاعدة بيانات PostgreSQL محلية

**الخطوات:**

1. **تثبيت Self-Hosted Runner:**
   ```bash
   # اذهب إلى: Settings → Actions → Runners → New self-hosted runner
   # اتبع التعليمات من GitHub
   ```

2. **إضافة Secret:**
   - `LOCAL_PG_PASSWORD` - كلمة مرور قاعدة البيانات المحلية

3. **تفعيل Workflow:**
   - الملف: `.github/workflows/update-json-self-hosted.yml`
   - سيعمل تلقائياً كل 15 دقيقة على الجهاز المحلي

---

## 🔧 الإعداد السريع

### للقاعدة البعيدة:

1. **أضف Secrets في GitHub:**
   ```
   PG_HOST=your-db-host.com
   PG_DATABASE=showroom_sales
   PG_USER=postgres
   PG_PASSWORD=your_password
   PG_PORT=5432
   PG_SSL=true
   ```

2. **Workflow سيعمل تلقائياً:**
   - كل 15 دقيقة
   - يولد `management_data.json`
   - يرفع التغييرات إلى GitHub

### للقاعدة المحلية:

1. **ثبت Self-Hosted Runner:**
   - اتبع: https://docs.github.com/en/actions/hosting-your-own-runners

2. **أضف Secret:**
   ```
   LOCAL_PG_PASSWORD=KhaKha11@
   ```

3. **Workflow سيعمل تلقائياً:**
   - كل 15 دقيقة على الجهاز المحلي
   - يولد `management_data.json`
   - يرفع التغييرات إلى GitHub

---

## 📊 النتيجة

بعد الإعداد، سترى commits تلقائية كل 15 دقيقة:
```
Auto Update 15m: Wed 01/28/2026 17:15:55.50
```

تماماً مثل المشروع المرجعي! 🎉

---

## ⚙️ التخصيص

### تغيير التوقيت:
عدّل `cron` في ملف workflow:
```yaml
schedule:
  - cron: '*/15 * * * *'  # كل 15 دقيقة
  - cron: '*/30 * * * *'  # كل 30 دقيقة
  - cron: '0 * * * *'     # كل ساعة
```

### تشغيل يدوي:
- اذهب إلى: `Actions` → `Auto Update JSON Data` → `Run workflow`

---

## ✅ Checklist

- [ ] اختيار الخيار المناسب (بعيد/محلي)
- [ ] إضافة Secrets في GitHub
- [ ] (للمحلي) تثبيت Self-Hosted Runner
- [ ] تفعيل Workflow
- [ ] التحقق من أول commit تلقائي

---

## 🐛 استكشاف الأخطاء

### Workflow لا يعمل:
- تحقق من أن Workflow مفعّل في `Actions`
- تحقق من Secrets
- تحقق من Logs في `Actions` → `Run workflow`

### لا يوجد commits:
- تحقق من اتصال قاعدة البيانات
- تحقق من أن الملفات تتغير
- تحقق من Git permissions

---

🎉 **بعد الإعداد، النظام سيعمل تلقائياً تماماً مثل المشروع المرجعي!**
