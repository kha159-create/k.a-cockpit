rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // قواعد مبسطة للاختبار - تسمح بجميع العمليات مؤقتاً
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
